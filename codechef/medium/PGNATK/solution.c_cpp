#include <stdio.h>

int main() {
    int T;
    if (scanf("%d", &T) == 1) {
        while (T--) {
            int N, K;
            scanf("%d %d", &N, &K)
            int tmin = N + (N - 1) / (K - 1);
            printf("%d\n", tmin);
        }
    }
    return 0;
}
